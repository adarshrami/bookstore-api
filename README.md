# bookstore-api
Bookstore REST API built using Spring Boot. Includes CRUD operations for Books and Authors, validation, pagination, sorting, and Swagger API documentation. Uses H2 database for testing.
